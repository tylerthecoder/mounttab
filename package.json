{
  "name": "extension",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.0.260"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}

